{% extends 'base.html.twig' %}

{% block title %}Ajouter un medecin{% endblock %}

{% block body %}
<h1>Ajouter un medecin</h1>
{{ form_start(form) }}
    {{ form_row(form.dateEmbauche) }}
    {{ form_row(form.specialite) }}
    <button class="btn btn-success">Enregistrer</button>
{{ form_end(form) }}
<a href="{{ path('medecin_index') }}" class="btn btn-secondary mt-3">Retour à la liste</a>
{% endblock %}