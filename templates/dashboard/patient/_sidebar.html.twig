<div class="p-4 text-white">
    {# ── User greeting (from JWT-authenticated user) ── #}
    {% if app.user %}
    <div class="text-center mb-4">
        {% if app.user.image %}
            <img src="/{{ app.user.image }}" alt="Photo" class="rounded-circle border border-2 border-white mb-2" style="width:64px;height:64px;object-fit:cover;">
        {% else %}
            <img src="https://ui-avatars.com/api/?name={{ app.user.name|url_encode }}+{{ app.user.lastName|url_encode }}&background=fff&color=667eea&size=64" alt="Avatar" class="rounded-circle border border-2 border-white mb-2" style="width:64px;height:64px;object-fit:cover;">
        {% endif %}
        <div class="fw-semibold" style="font-size:.95rem;">👋 Hello, Admin {{ app.user.lastName }}</div>
    </div>
    <hr class="border-white opacity-50 mt-0 mb-3">
    {% endif %}

    <h4 class="mb-4 fw-bold">🩺 Patient Panel</h4>
    <ul class="nav flex-column gap-2">
        <li class="nav-item">
            <a class="nav-link text-white rounded-3 px-3 py-2" href="{{ path('patient_dashboard') }}">
                <i class="bi bi-house-fill"></i> Accueil
            </a>
        </li>
        <li class="nav-item">
            <a class="nav-link text-white rounded-3 px-3 py-2" href="{{ path('patient_rendezvous') }}">
                <i class="bi bi-calendar-check-fill"></i> Mes rendez-vous
            </a>
        </li>
        <li class="nav-item">
            <a class="nav-link text-white rounded-3 px-3 py-2" href="{{ path('patient_profil') }}">
                <i class="bi bi-person-fill"></i> Mon profil
            </a>
        </li>
    </ul>
</div>