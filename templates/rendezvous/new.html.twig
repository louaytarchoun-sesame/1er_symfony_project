{% extends 'base.html.twig' %}

{% block title %}Ajouter un rendez-vous{% endblock %}

{% block body %}
<h1>Ajouter un rendez-vous</h1>
{{ form_start(form) }}
    {{ form_row(form.date) }}
    {{ form_row(form.duree) }}
    {{ form_row(form.motif) }}
    {{ form_row(form.etat) }}
    {{ form_row(form.patient) }}
    {{ form_row(form.medecin) }}
    <button class="btn btn-success">Enregistrer</button>
{{ form_end(form) }}
<a href="{{ path('rendezvous_index') }}" class="btn btn-secondary mt-3">Retour à la liste</a>
{% endblock %}
