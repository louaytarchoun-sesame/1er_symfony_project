<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{% block title %}Auth - Gestionnaire médical{% endblock %}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">
    {% block stylesheets %}{% endblock %}
    {% block importmap %}{{ importmap('app') }}{% endblock %}
    <style>
      .navbar-brand-icon {
        width: 38px; height: 38px;
        display: inline-flex; align-items: center; justify-content: center;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        border-radius: .6rem;
        color: #fff;
        font-size: 1.1rem;
        margin-right: .5rem;
      }
      .nav-cta:hover { opacity: .9; }
    </style>
  </head>
  <body class="bg-light">
    <nav class="navbar navbar-expand-lg bg-white shadow-sm sticky-top py-2">
      <div class="container">
        <a class="navbar-brand d-flex align-items-center fw-bold text-dark" href="{{ path('home') }}">
          <span class="navbar-brand-icon"><i class="bi bi-heart-pulse-fill"></i></span>
          MedGestion
        </a>

        <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" data-bs-target="#navMain">
          <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navMain">
          <ul class="navbar-nav mx-auto gap-1">
            <li class="nav-item">
              <a class="nav-link fw-semibold" href="{{ path('home') }}"><i class="bi bi-house-door me-1"></i>Accueil</a>
            </li>
            <li class="nav-item">
              <a class="nav-link fw-semibold" href="#features"><i class="bi bi-stars me-1"></i>Fonctionnalités</a>
            </li>
            <li class="nav-item">
              <a class="nav-link fw-semibold" href="#how"><i class="bi bi-signpost-split me-1"></i>Comment ça marche</a>
            </li>
          </ul>

          <div class="d-flex gap-2 mt-2 mt-lg-0">
            <a class="btn btn-outline-primary rounded-pill px-3" href="{{ path('app_login') }}">
              <i class="bi bi-box-arrow-in-right me-1"></i>Se connecter
            </a>
            <a class="btn btn-primary rounded-pill px-3 nav-cta" href="{{ path('app_register') }}"
               style="background:linear-gradient(135deg,#667eea,#764ba2);border:none;">
              <i class="bi bi-person-plus me-1"></i>Créer un compte
            </a>
          </div>
        </div>
      </div>
    </nav>

    <main class="container py-5">
      {% block body %}{% endblock %}
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    {% block javascripts %}{% endblock %}
  </body>
</html>
